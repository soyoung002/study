# 학습목표 #

클래스와 객체의 차이 이해하기
자바에서 클래스를 정의하고 객체를 생성하는 방법을 학습한다.
객체의 속성과 메서드에 접근하는 방법을 배웁니다.

# 주요 핵심 키워드와 개념 #
- 클래스(Class) : 객체를 생성하기 위한 '틀' 또는 '설계도'. 공통적인
- 객체(Object) : 클래스에 기반하여 생성된 '실체'. 
				각 객체는 독립적인 상태와 행동을 가집니다.
- 속성(Attribute) : 객체의 상태를 나타내는 변수.
- 메서드(Method) : 객체의 행동을 나타내는 함수.

# 토론해볼 내용 #
- 클래스와 객체는 실제 세계의 어떤 것들과 유사한가요?
- 객체 지향 프로그래밍에서 클래스와 객체의 중요성은 무엇인가요?
- 실제 프로젝트에서 클래스와 객체를 효율적으로 사용하기 위한 전략은 무엇인가요?



//간단한 퀴즈//

1) 클래스는 객체의 ___이며, 객체의 클래스의 ___입니다. c
	a. 복사본, 원본
	b. 틀, 실체
	c. 부모, 자식
	d. 메서드, 속성
	
2) 'Dog' 클래스가 있고 'bark'라는 메서드를 포함하고 있다면,
'retriever'라는 Dog 객체가 'bark' 메서드를 사용할수 있을까요? a
	a. 예
	b. 아니요	
