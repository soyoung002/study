* 학습목표
- 자바의 클래스와객체 개념 이해
- 클래스를 어떻게 정의하며, 객체를 어떻게 생성하는지 배우기
- 클래스의 필드와 메소드의 기능을 파악
- 객체 지향 프로그래밍 기본 개념에 대한 이해

1. 클래스는 어떤 것을 나타냅니까?
	a)실제 세계의 객체
	b)객체의 실체
	c)객체를 생성하기 위한 설계도
	d)프로그램의 실행 파일
	
2.new 키워드를 사용하면 어떤 작업이 이루어집니까?
	a)샐ㅗ운 클래스를 정의합니다
	b)클래스의 메소드를 호출합니다
	c)새로운 객체를 생성합니다
	d)기존 객체를 삭제합니다
	
# 주요 핵심 키워드와 개념
	- 클래스(class) : 객체를 생성하기 위한 템틀릿 또는 설계도
					데이터(필드)와 이를 조작하는 동작(메소드를) 정의합니다.
	- 객체(Object) : 클래스를 통해 실체화된 인스턴스
					실제로 메모리에 할당된 상태입니다.
	- 필드(Field) : 클래스 내부에 정의된 변수로, 객체의 상태 또는 속성
	- 메소드(Method) : 클래스에서 정의한 동작이나 행위.
					로직과 기능을 수행
	- 생성자(Constructor) : 객체의 초기화를 위한 특별한 메소드
							클래스 이름과 동일하며, 객체가 생성될 때 자동으로 호출됨.
